export enum NotificationType {
  AWAIT_REVIEWER = "NotificationType.AWAIT_REVIEWER",
  AWAIT_AUTO_MERGE = "NotificationType.AWAIT_AUTO_MERGE",
  REVISION_NEEDED = "NotificationType.REVISION_NEEDED",
  UPDATE = "NotificationType.UPDATE",
}
